# SPEC: <feature name>

> Copy to `SPEC.md` (or `docs/specs/<feature>.md`) and fill in. The best specs are
> self-contained: name the files and interfaces, state what's out of scope, and end
> with an end-to-end check that proves it works. Time spent here beats time spent
> watching the implementation.
>
> Fast path: start a session and say *"Interview me about <feature> using
> AskUserQuestion, then write the spec here."* Then run the spec in a fresh session.

## Goal

<!-- One or two sentences. What does "done" look like for the user? -->

## Context

<!-- Why now? Relevant existing code, prior art, constraints. -->

## Approach

<!-- The chosen design. Files/interfaces involved. Key decisions and tradeoffs. -->

- Files to touch:
- New interfaces / functions:
- Dependencies (prefer existing — see the decision hierarchy):

## Out of scope

<!-- Explicitly list what this will NOT do, to prevent scope creep. -->

## Edge cases

<!-- The hard parts. Each should map to a test below. -->

## Verification (success criteria)

<!-- Concrete, runnable checks. This is what "done" means. -->

- [ ] Tests:
- [ ] Build / typecheck passes:
- [ ] Manual / e2e check:
- [ ] Evidence to capture:
